<div class="container-fluid">
  <p style="color: green"><%= notice %></p>

  <%= render @player %>

  <div class="container-fluid">
    <%= button_to "Edit this player", edit_player_path(@player), :class => "btn btn-warning" %>
    <%= button_to "Back to players", players_path, :class => "btn btn-info" %>
    <%= button_to "Add a new member", new_player_path, :class => "btn btn-secondary" %>
  </div>

  <div class="container-fluid">
    <%= button_to "Delete this player", @player, method: :delete, :class => "btn btn-danger" %>
  </div>
</div>